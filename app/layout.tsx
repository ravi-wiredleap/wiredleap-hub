import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "WiredLeap - Art of the Possible",
  description: "Discover the future of AI-powered visual intelligence for public safety, smart cities, and enterprise",
  keywords: ["AI", "Computer Vision", "Smart Cities", "Public Safety", "Enterprise AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
